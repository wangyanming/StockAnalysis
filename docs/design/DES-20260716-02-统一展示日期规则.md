# 方案设计：统一展示日期规则

**版本:** v1.0
**日期:** 2026-07-16
**关联需求:** REQ-20260716-02-统一展示日期规则
**状态:** 草稿

## 1. 总体方案

在 `utils/date_utils.py` 新增 `get_display_date()` 函数，统一所有接口的自动日期选择行为。前端 6 个接口统一调用此函数替代各自的日期逻辑。

## 2. 新增文件

### `utils/date_utils.py`

```python
"""
日期工具函数

提供 get_display_date() 统一展示日期规则：
- 17:00 前 → 展示 T-1 日（昨天收盘数据）
- 17:00 后 → 展示 T 日（今天收盘数据）
- 非交易日自动往前递推
"""

from datetime import datetime, timedelta
from typing import Optional


def _is_trade_date(date_str: str) -> bool:
    """判断日期是否为交易日（在 stock_daily 表有数据）"""
    from utils.dao import get_db
    db = get_db()
    row = db.fetchone(
        "SELECT 1 FROM stock_daily WHERE trade_date = %s LIMIT 1",
        (date_str,)
    )
    return row is not None


def get_display_date() -> str:
    """
    返回当前应展示的日期（YYYYMMDD 格式）
    
    规则：
    - 当前时间 ≥ 17:00 → 目标 = 今天（T 日）
    - 当前时间 < 17:00 → 目标 = 昨天（T-1 日）
    - 如果目标日期不是交易日，逐日往前找到最近有数据的交易日
    """
    now = datetime.now()
    
    # 确定目标日期
    if now.hour >= 17:
        target = now
    else:
        target = now - timedelta(days=1)
    
    # 非交易日往前递推（最多推 30 天防止死循环）
    for _ in range(30):
        target_str = target.strftime('%Y%m%d')
        if _is_trade_date(target_str):
            return target_str
        target -= timedelta(days=1)
    
    # 兜底：返回 30 天前的日期（极小概率触发）
    return target.strftime('%Y%m%d')
```

## 3. 修改文件

### `web_server.py`（6 处修改）

| 方法 | 改动内容 |
|------|---------|
| `api_market_overview` | 不再调新浪实时。改为从 `index_quotes` 表按 `get_display_date()` 查当天 5 个指数的收盘记录 |
| `api_market_summary` | 传给 `f.get_market_summary()` 时用 `get_display_date()` 替代 `datetime.now().strftime` |
| `api_sectors` | `last_date` 改为 `get_display_date()`，不再 `MAX(record_date)` |
| `api_limit_up` | 默认日期从 `now.strftime` 改为 `get_display_date()` |
| `api_limit_down` | 同上 |
| `api_limit_up_industry` | 同上 |

### `utils/stock_analysis_api.py`（1 处修改）

| 方法 | 改动内容 |
|------|---------|
| `get_market_summary` | 去掉内部的 `date_str or datetime.now()` 逻辑，改为调用 `get_display_date()`（保持参数兼容） |

## 4. 规则详细说明

| 时间 | 目标日期 | 说明 |
|------|---------|------|
| 周一 09:00 | 上周五 | 17:00前→T-1=周日，非交易日→递推至周五 |
| 周三 15:00 | 周三或周二 | 17:00前→T-1=周二 |
| 周三 17:00 | 周三 | 17:00后→周三 |
| 春节假期前最后交易日 | 假期期间→该交易日 | 逐日递推自动跳过长假 |

## 5. 验证标准

1. `get_display_date()` 周一 09:00 → 上周五
2. `get_display_date()` 周三 15:00 → 周二
3. `get_display_date()` 周三 17:00 → 周三
4. 6 个接口全部返回 200，数据正常
5. `bash tests/check_engineering.sh` 通过
